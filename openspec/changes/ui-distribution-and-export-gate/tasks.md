# Tasks: Review Layout Distribution + Unified Export Gate

1. [x] Proposal
2. [x] Specification
3. [x] Design
4. [ ] **TDD R3: unified export gate** — RED parity test (report-None + all-removed), add
   `export_allowed`, route nav + both view models through it.
5. [ ] **TDD R1: Review QSplitter** — RED test asserting `tables_splitter` holds the 3 tables; wrap
   sections, remove trailing stretch.
6. [ ] **TDD R2: history_table min/max** — RED test asserting `min <= max` under compact layout; fix.
7. [ ] **VERIFY** — full suite, pyright, coverage, ruff, release gate.
8. [ ] **Commit + push** to the open PR branch.
