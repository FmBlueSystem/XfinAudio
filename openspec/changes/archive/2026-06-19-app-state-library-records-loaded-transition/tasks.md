# Tasks

1. RED: Add a failing transition unit test for immutable loaded library records and lookup rebuild.
2. GREEN: Implement `apply_library_records_loaded()` and export it.
3. REFACTOR: Wire `LibraryController.populate_track_table()` and persisted restore through the transition.
4. VERIFY: Run focused tests, full gates, and archive the OpenSpec change.
