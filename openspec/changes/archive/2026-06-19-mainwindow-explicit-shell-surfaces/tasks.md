# Tasks

- [x] 1. RED: Add focused coverage proving `MainWindow` no longer imports/references the `shell_compat` facade.
- [x] 2. GREEN: Update `MainWindow` to use `shell_layout_compat` and `shell_state_compat` directly.
- [x] 3. REFACTOR: Keep `shell_compat.py` as a stable facade and preserve existing facade tests.
- [x] 4. VERIFY: Run focused tests and full project gates.
- [x] 5. ARCHIVE HANDOFF: Leave the verified change ready for archive after merge.
