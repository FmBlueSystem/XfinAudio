# DJ Readiness Summary Application Boundary Design

Extend `xfinaudio.application.dj_readiness` with `format_application_dj_readiness_summary()`, a thin application boundary over the existing quality formatter. Update desktop controllers to import only the application formatter for this display responsibility.
