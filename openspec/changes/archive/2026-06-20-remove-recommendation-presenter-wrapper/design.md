# Remove Recommendation Presenter Wrapper Design

Delete `src/xfinaudio/desktop/recommendation_presenter.py`. Keep existing candidate-pool tests in place because they already import `xfinaudio.recommendation.candidate_pool` directly.
