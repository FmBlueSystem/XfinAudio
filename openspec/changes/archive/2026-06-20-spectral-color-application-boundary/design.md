# Spectral Color Application Boundary Design

Add `xfinaudio.application.spectral_profile_display.format_application_spectral_color()` as a thin display boundary over the existing audio formatter. Update desktop display/view-model modules to import only the application formatter for this responsibility.
