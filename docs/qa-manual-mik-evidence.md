# XfinAudio — Real Mixed In Key QA Evidence

<!-- MIK-QA-STATUS: pending -->

This document records manual QA evidence for XfinAudio against a real Mixed In
Key processed audio library. The automated part is generated by
`scripts/manual_mik_qa_harness.py`; the maintainer must review the results and
mark the sign-off checkboxes.

## How to generate evidence

1. Ensure you have a folder of audio files already processed by Mixed In Key.
2. Run the harness from the repository root:

   ```bash
   uv run python scripts/manual_mik_qa_harness.py /path/to/mik/library --git-commit
   ```

3. Review the generated sections below.
4. Mark the sign-off checkboxes at the bottom.
5. Commit this file so `release_gate_check.py` reports the gate as completed.

## Generated evidence

No evidence generated yet. Run the harness above to populate this section.

## Maintainer sign-off

- [ ] I inspected the scan results and they match the source folder.
- [ ] I inspected at least one generated Serato crate in a hex/text viewer.
- [ ] I verified that no files were written outside the temporary evidence folder.
- [ ] I confirm this evidence was generated against a real Mixed In Key processed library.
